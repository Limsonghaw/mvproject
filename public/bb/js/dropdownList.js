function changeType(Type) {
    var Status = document.getElementById('Status');
    Status.textContent = Type;
}

function changeType1(Type) {
    var sendTo = document.getElementById('sendTo');
    sendTo.textContent = Type;
}

function changeType2(Type) {
    var sendStatus = document.getElementById('Send');
    sendStatus.textContent = Type;
}

function changeType3(Type) {
    var date = document.getElementById('Date');
    date.textContent = Type;
}