function changeType(Type) {
    var Types = document.getElementById('Type');
    Types.textContent = Type;
}

function changeType1(Type) {
    var Types = document.getElementById('sendTo');
    Types.textContent = Type;
}

function changeType2(Type) {
    var Types = document.getElementById('Status');
    Types.textContent = Type;
}

function changeType3(Type) {
    var Payment = document.getElementById('Payment');
    Payment.textContent = Type;
}